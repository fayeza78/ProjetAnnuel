docker compose up -d

npm init -y && npm i -D typescript && npx tsc -init && npm i -D tsx


# Dépendance
    // Express.js installation permet de créer un serveur web pour gérer les requêtes HTTP et les routes de l'API.
    npm install express
    npm install --save-dev @types/express // @types/express est une bibliothèque de types TypeScript pour Express.js, qui fournit des définitions de types pour les fonctionnalités d'Express, facilitant ainsi le développement avec TypeScript.

    // Permet d'evite de faire les requete SQL
    npm install typeorm 

    npm install pg mongodb neo4j-driver dotenv
